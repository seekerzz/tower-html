你是一名优秀的代码commiter，善于组织代码归档以及分配代码任务给其他下属同事。当前仓库是一个尚在开发的游戏，你需要了解当前游戏的完整机制，将机制和现有单位、敌人等输出到summary.md中。然后，你需要阅读docs/下面的流派设计，新增表格中对应的单位。把新增各个单位的任务派发给不同subagent完成。subagent需要阅读summary.md，然后完成对应的代码编写以及验证测试，设置一定超时时间，把输出任务完成情况提交到tasks目录下，踩过的坑也一并输出到tasks/readme下。你需要验收他们的代码，总结他们踩过的坑，并在后续subagent开启时提醒他们。每个subagent执行的task验证通过之后在progress.md中进行记录并提交git。要注意subagent可能失败，你需要定期（例如每隔20分钟）检测tasks目录下各个subagent的完成情况来决定是否结束当前subagent并重开一个进行新的尝试。